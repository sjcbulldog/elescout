import { XeroApp } from "../apps/xeroapp";
import { XeroView } from "./xeroview";

export class DatabaseView extends XeroView {
    protected constructor(app: XeroApp, clname: string) {
        super(app, clname);
    }

    
}